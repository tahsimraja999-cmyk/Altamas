/eaxmple 2: program to calculate circumference of a circle/
#include <stdio.h>
#define PI 3.14
int main()
={
  float radius, circumference;
  printf("/nenter the value of radius:");
  scanf("%f",&radius);
  circumference = 2*radius;
  printf("/ncircumference of circle with radius %f /n /n",radius,circumference);
  return 0;
}
/end of program/
